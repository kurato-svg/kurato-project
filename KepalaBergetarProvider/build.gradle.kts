version = 1

cloudstream {
    description = "KepalaBergetar — Drama Melayu"
    language = "ms"
    authors = listOf("Kurato")

    status = 3

    tvTypes = listOf(
        "TvSeries",
        "Movie"
    )

    iconUrl = "https://kepalabergetar9.pro/favicon.ico"
}
